/* 주어진 유향 그래프에서 특정한 두 노드 간에 경로가 존재하는지 
 * 판별하는 알고리즘을 구현하라  
 *
 * 결국에 전수 조사하는 방법으로 구현해야 할 것 같다.
 * dfs, bfs 중 택일하자..
 * */

#include <iostream>
#include <string>
#include "tree.h"

using namespace std;

typedef BinTreeNode<int> intnode;

int main() {
    intnode *a = new intnode;
    intnode *b = new intnode;
    intnode *c = new intnode;
    intnode *d = new intnode;
    intnode *e = new intnode;
}


