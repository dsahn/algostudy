/* 주어진 이진 트리에서 깊이별로 연결 리스트를 만들어 내는 알고리즘을 작성하라
 * (트리에서 깊이가 D라면, 알고리즘 수행 결과로 D개의 연결 리스트가 만들어져야
 * 한다.)
 *
 * 문제가 잘 이해가 안된다... bfs로 레벨마다의 리스트를 만드는건지? 그렇다면
 * 괄호 안의 문장이 안맞기 때문에..
 * */
#include <iostream>
#include <string>
#include "tree.h"
#include "../2-linked-list/linked_list.h"

using namespace std; 
typedef BinTreeNode<int> intnode;

int main() {
    return 0;
}
